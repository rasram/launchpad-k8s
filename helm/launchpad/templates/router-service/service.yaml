apiVersion: v1
kind: Service
metadata:
  name: router-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: router-service
    {{- include "launchpad.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: router-service
  ports:
    - name: http
      port: {{ .Values.routerService.port }}
      targetPort: http
